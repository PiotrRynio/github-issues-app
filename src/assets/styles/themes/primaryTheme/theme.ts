import {
  breakpoints,
  colors,
  fontFamilies,
  fontSizes,
  fontWeights,
  lineHeights,
  sizes,
  typographies,
} from './themeValues';
import { mixins } from './mixinsValues/mixins';
import { Theme } from '../types';

export const theme: Theme = {
  breakpoints,
  colors,
  fontFamilies,
  fontSizes,
  fontWeights,
  lineHeights,
  mixins,
  sizes,
  typographies,
};
